import { createMuiTheme } from '@material-ui/core/styles';

const lvpOptions = {};
export const lvpTheme = createMuiTheme(lvpOptions);

const mvpOptions = {};
export const lvpTheme = createMuiTheme(mvpOptions);

const svpOptions = {};
export const lvpTheme = createMuiTheme(svpOptions);
